channels {
    is_default = true
    name = "Default"
}

channels {
    description = ""
    name = "Another Channe l"

    rules {
        tag = "a"
        version_range = "[1.0.0]"

        deployment_action_packages "hello-world-using-powershell" "putty" {
        }
    }
}
