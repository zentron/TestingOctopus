channels {
    is_default = true
    name = "Default"
}

channels {
    description = ""
    name = "Another Channel"

    rules {
        tag = ""
        version_range = "[1.0.0]"

        deployment_action_packages "Hello world (using PowerShell)" "putty" {
        }
    }
}