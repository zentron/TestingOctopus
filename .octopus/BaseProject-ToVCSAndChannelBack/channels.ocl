channel "Default" {
    is_default = true
}

channel "Second Channal!!!" {
    description = ""
    lifecycle = "Just Dev"

    rules {
        tag = ""
        version_range = "[0,22]"

        deployment_action_packages {
            package = "putty"
            step = "Hello world (using PowerShell)"
        }
    }
}