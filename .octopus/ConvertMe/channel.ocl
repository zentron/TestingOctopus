channels {
    is_default = true
    name = "Default"
}

channels {
    description = ""
    name = "Secondary"

    rules {
        tag = "ddd"
        version_range = ""

        deployment_action_packages "Hello world (using PowerShell)" "FooBar" {
        }
    }
}